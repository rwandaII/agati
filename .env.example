# Local: `docker compose up -d` gives you exactly this one.
# Deployed: the pooled connection string, with ?sslmode=require&pgbouncer=true
DATABASE_URL="postgresql://agati:agati@localhost:5434/agati?schema=public"
SESSION_SECRET="change-me-to-32-plus-random-bytes-please"
APP_URL="http://localhost:3000"

# Payments - absent = Mock provider, everything still works
FLW_ENV="sandbox"
FLW_CLIENT_ID=""
FLW_CLIENT_SECRET=""
FLW_SECRET_HASH=""
