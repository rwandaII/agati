# local: matches docker-compose.yml
# deployed: the pooled string + ?sslmode=require&pgbouncer=true
DATABASE_URL="postgresql://agati:agati@localhost:5434/agati?schema=public"
SESSION_SECRET="change-me-to-32-plus-random-bytes-please"
APP_URL="http://localhost:3000"

# leave the FLW_ values empty to run payments in mock mode
FLW_ENV="sandbox"
FLW_CLIENT_ID=""
FLW_CLIENT_SECRET=""
FLW_SECRET_HASH=""
